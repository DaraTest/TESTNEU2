name: .NET Framework CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Restore dependencies
      run: nuget restore
    - name: Build
      run: msbuild TESTneu2.sln /p:Configuration=Release
    - name: Test
      run: |
        vstest.console.exe **\*Tests.dll
